/**
 * AppError utility (alias for ErrorResponse)
 * This file exists for backward compatibility with code that imports AppError
 */
const ErrorResponse = require('./errorResponse');

module.exports = ErrorResponse; 