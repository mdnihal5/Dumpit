/**
 * CatchAsync utility (alias for asyncHandler)
 * This file exists for backward compatibility with code that imports catchAsync
 */

const asyncHandler = require('./asyncHandler');

module.exports = asyncHandler; 